const path = require("path");

// Path.join joins a sequence of path parts using separator (whatever shows for path.sep)
const join = path.join("fake_path", "next_fake", "last_fake");

console.log(join);
